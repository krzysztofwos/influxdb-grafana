apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    user: $INFLUXDB_USER
    database: $INFLUXDB_DB
    isDefault: true
    jsonData:
      httpMode: GET
    secureJsonData:
      password: $INFLUXDB_USER_PASSWORD
    editable: true
